{
  "name": "backbone-app",
  "version": "1.0.0",
  "description": "Backbone App",
  "main": "src/index.js",
  "scripts": {
    "test": "npx playwright test tests/**.spec.js",
    "build:browser:watch": "budo src/index.js --live",
    "build:browser:full": "mkdir -p dist && browserify src/index.js > dist/exe.js && uglifyjs dist/exe.js --compress --mangle > dist/exe.min.js",
    "build:browser:app": "mkdir -p dist && browserify src/app/index.js > dist/app.js && uglifyjs dist/app.js --compress --mangle > dist/app.min.js"
  },
  "keywords": [],
  "license": "AGPL-3.0-or-later",
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.21.1",
    "browserify": "^17.0.0",
    "budo": "^11.7.0",
    "uglify-js": "^3.15.4"
  }
}
