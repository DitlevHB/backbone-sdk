{
  "name": "backbone-app",
  "version": "1.0.0",
  "description": "Backbone App",
  "main": "src/index.js",
  "scripts": {
    "test": "npx playwright test test/**.spec.js",
    "build:browser:dev": "mkdir -p dist && browserify --debug src/index.js > dist/app.dev.js",
    "build:browser:watch": "budo src/index.js --live",
    "build:browser": "mkdir -p dist && browserify src/index.js > dist/core.js && uglifyjs dist/core.js --compress --mangle > dist/core.min.js"
  },
  "keywords": [],
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "@backbonedao/core": "github:backbonedao/crypto"
  },
  "devDependencies": {
    "@playwright/test": "^1.20.2",
    "browserify": "^17.0.0",
    "budo": "^11.7.0",
    "uglify-js": "^3.15.4"
  }
}
